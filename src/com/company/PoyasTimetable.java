package com.company;

import javax.swing.*;

public class PoyasTimetable {
    public void create(JFrame frame0) {
        JFrame frame = new JFrame("FedTimetable");
        frame.setLocation(600, 300);
        frame.setSize(300, 300);
        frame.setVisible(true);
    }
}
